# Additional notes added: CI now runs Python-based linting & tests, sentinel provides /healthz and /readyz endpoints, and sync-agent uses improved retry and idempotent git handling.

See .github/workflows/ci.yml for CI configuration.
